# 工作日志 - 第13天

**姓名**：宋明杰  
**组别**：第20组  
**日期**：2026年6月14日

## 一、今日工作内容

### 1. 竞赛答题后端开发
- ✅ 开发QuizController竞赛控制器
- ✅ 开发QuizService业务逻辑层
- ✅ 开发QuizMapper数据访问层
- ✅ 实现竞赛列表查询
- ✅ 实现竞赛详情查询

### 2. 答题功能开发
- ✅ 实现开始答题接口
- ✅ 实现获取题目接口（随机抽题）
- ✅ 实现提交答卷接口
- ✅ 实现自动判分逻辑

### 3. 答题记录功能
- ✅ 实现答题记录查询
- ✅ 实现答题详情查询
- ✅ 实现历史成绩查询
- ✅ 实现成绩排行功能

## 二、技术实现要点

### 开始答题
```java
@Transactional
public QuizRecord startQuiz(Long userId, Long quizId) {
    Quiz quiz = quizMapper.selectById(quizId);
    if (quiz == null) {
        throw new BusinessException("竞赛不存在");
    }
    
    // 创建答题记录
    QuizRecord record = new QuizRecord();
    record.setUserId(userId);
    record.setQuizId(quizId);
    record.setTotalScore(quiz.getTotalScore());
    record.setTotalCount(quiz.getQuestionCount());
    record.setStartTime(new Date());
    record.setStatus("in_progress");
    quizRecordMapper.insert(record);
    
    return record;
}
```

### 获取题目（随机抽题）
```java
public List<Question> getQuestions(Long recordId) {
    QuizRecord record = quizRecordMapper.selectById(recordId);
    if (record == null || !"in_progress".equals(record.getStatus())) {
        throw new BusinessException("答题记录无效");
    }
    
    // 随机获取题目
    List<Question> questions = questionMapper.selectList(
        new QueryWrapper<Question>()
            .eq("quiz_id", record.getQuizId())
            .orderByAsc("RAND()")
            .last("LIMIT " + record.getTotalCount())
    );
    
    // 隐藏答案和解析
    questions.forEach(q -> {
        q.setAnswer(null);
        q.setAnalysis(null);
    });
    
    return questions;
}
```

### 提交答卷与判分
```java
@Transactional
public QuizRecord submitQuiz(Long recordId, Map<Long, String> userAnswers) {
    QuizRecord record = quizRecordMapper.selectById(recordId);
    if (record == null || !"in_progress".equals(record.getStatus())) {
        throw new BusinessException("答题记录无效");
    }
    
    // 获取所有题目和正确答案
    List<Question> questions = questionMapper.selectList(
        new QueryWrapper<Question>().eq("quiz_id", record.getQuizId())
    );
    
    int score = 0;
    int correctCount = 0;
    
    // 判分
    for (Question question : questions) {
        String userAnswer = userAnswers.get(question.getId());
        boolean isCorrect = question.getAnswer().equals(userAnswer);
        
        if (isCorrect) {
            correctCount++;
            score += question.getScore();
        }
        
        // 保存答题详情
        QuizRecordDetail detail = new QuizRecordDetail();
        detail.setRecordId(recordId);
        detail.setQuestionId(question.getId());
        detail.setUserAnswer(userAnswer);
        detail.setIsCorrect(isCorrect ? 1 : 0);
        quizRecordDetailMapper.insert(detail);
    }
    
    // 更新答题记录
    record.setScore(score);
    record.setCorrectCount(correctCount);
    record.setEndTime(new Date());
    record.setStatus("completed");
    quizRecordMapper.updateById(record);
    
    return record;
}
```

### 成绩排行
```java
public List<QuizRecord> getRanking(Long quizId, Integer limit) {
    return quizRecordMapper.selectList(
        new QueryWrapper<QuizRecord>()
            .eq("quiz_id", quizId)
            .eq("status", "completed")
            .orderByDesc("score")
            .orderByAsc("end_time") // 分数相同，按完成时间排序
            .last("LIMIT " + limit)
    );
}
```

## 三、完成情况

| 模块 | 进度 | 备注 |
|------|------|------|
| 竞赛管理 | 100% | 列表详情查询 |
| 答题功能 | 100% | 抽题判分完成 |
| 记录查询 | 100% | 历史记录排行 |

## 四、API接口清单

1. GET /quiz/list - 获取竞赛列表
2. GET /quiz/{id} - 获取竞赛详情
3. POST /quiz/start/{quizId} - 开始答题
4. GET /quiz/questions/{recordId} - 获取题目
5. POST /quiz/submit/{recordId} - 提交答卷
6. GET /quiz/records - 获取我的答题记录
7. GET /quiz/records/{id} - 获取答题详情
8. GET /quiz/ranking/{quizId} - 获取成绩排行

## 五、遇到的问题与解决方案

**问题1**：随机抽题性能慢  
**解决**：使用ORDER BY RAND()在MySQL中随机排序，数据量大时可优化为应用层随机

**问题2**：判分逻辑复杂（多选、判断）  
**解决**：统一使用字符串比对，多选题答案排序后比对（如"A,B,C"）

**问题3**：答题中途退出不保存进度  
**解决**：后续可增加定时保存答案功能，支持断点续答

## 六、明日计划

1. 完成海洋知识个人中心页面搭建
2. 实现用户海洋答题记录查看
3. 实现个人信息编辑功能
4. 完成答题页面与题库数据的前后端联调
