# 工作日志 - 第5天

**姓名**：宋明杰  
**组别**：第20组  
**日期**：2026年6月6日

## 一、今日工作内容

### 1. 文件上传模块
- ✅ 开发FileController文件控制器
- ✅ 实现文件上传接口
- ✅ 支持图片、PDF等文件格式
- ✅ 实现文件大小限制（10MB）
- ✅ 实现文件类型校验

### 2. 文件存储方案
- ✅ 配置文件存储路径
- ✅ 实现文件重命名（UUID避免冲突）
- ✅ 实现文件分类存储（按日期分目录）
- ✅ 生成文件访问URL

### 3. 文件下载功能
- ✅ 实现文件下载接口
- ✅ 设置响应头（Content-Type、Content-Disposition）
- ✅ 支持图片预览

## 二、技术实现要点

### 文件上传实现
```java
@PostMapping("/upload")
public CommonResp<String> upload(@RequestParam("file") MultipartFile file) {
    // 校验文件大小
    if (file.getSize() > 10 * 1024 * 1024) {
        throw new BusinessException("文件大小不能超过10MB");
    }
    
    // 校验文件类型
    String originalFilename = file.getOriginalFilename();
    String extension = originalFilename.substring(originalFilename.lastIndexOf("."));
    if (!ALLOWED_EXTENSIONS.contains(extension)) {
        throw new BusinessException("不支持的文件类型");
    }
    
    // 生成新文件名
    String newFileName = UUID.randomUUID() + extension;
    
    // 按日期创建目录
    String dateDir = LocalDate.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd"));
    String destPath = uploadPath + "/" + dateDir + "/" + newFileName;
    
    // 保存文件
    File dest = new File(destPath);
    file.transferTo(dest);
    
    // 返回访问URL
    String fileUrl = "/file/" + dateDir + "/" + newFileName;
    return new CommonResp<>(fileUrl);
}
```

### 文件下载实现
```java
@GetMapping("/download/{dateDir}/{fileName}")
public void download(@PathVariable String dateDir, 
                     @PathVariable String fileName,
                     HttpServletResponse response) {
    File file = new File(uploadPath + "/" + dateDir + "/" + fileName);
    response.setContentType("application/octet-stream");
    response.setHeader("Content-Disposition", "attachment;filename=" + fileName);
    Files.copy(file.toPath(), response.getOutputStream());
}
```

## 三、完成情况

| 模块 | 进度 | 备注 |
|------|------|------|
| 文件上传 | 100% | 支持多种格式 |
| 文件存储 | 100% | 按日期分类 |
| 文件下载 | 100% | 支持预览 |

## 四、API接口清单

1. POST /file/upload - 文件上传
2. GET /file/download/{dateDir}/{fileName} - 文件下载
3. GET /file/preview/{dateDir}/{fileName} - 文件预览

## 五、遇到的问题与解决方案

**问题1**：上传大文件时内存溢出  
**解决**：使用MultipartFile.transferTo()直接写入磁盘，避免加载到内存

**问题2**：文件名中文乱码  
**解决**：使用UUID重命名文件，避免中文文件名问题

**问题3**：并发上传同名文件冲突  
**解决**：使用UUID生成唯一文件名，彻底避免冲突

## 六、明日计划

1. 开发用户笔记模块
2. 实现笔记增删改查功能
3. 实现笔记公开/私密设置
