# 工作日志 - 第2天

**姓名**：宋明杰  
**组别**：第20组  
**日期**：2026年6月3日

## 一、今日工作内容

### 1. 用户认证模块开发
- ✅ 开发用户注册接口（UserController.register）
- ✅ 开发用户登录接口（UserController.login）
- ✅ 实现密码加密（BCrypt算法）
- ✅ 开发密码重置功能

### 2. JWT认证机制
- ✅ 引入JWT依赖（jjwt 0.9.1）
- ✅ 开发JwtUtil工具类
- ✅ 实现Token生成、解析、验证功能
- ✅ 配置Token过期时间（24小时）

### 3. 拦截器开发
- ✅ 开发LoginInterceptor登录拦截器
- ✅ 开发AdminInterceptor管理员拦截器
- ✅ 配置WebMvcConfig拦截器注册
- ✅ 实现白名单机制（登录、注册接口放行）

## 二、技术实现要点

### JWT Token结构
```
Header: {"alg": "HS256", "typ": "JWT"}
Payload: {"userId": 1, "loginName": "admin", "role": "admin"}
Signature: HMACSHA256(base64UrlEncode(header) + "." + base64UrlEncode(payload), secret)
```

### 密码加密方案
- 采用BCrypt算法替代MD5
- 自动生成盐值，提高安全性
- 使用spring-security-crypto库

### 拦截器配置
```java
registry.addInterceptor(loginInterceptor)
    .addPathPatterns("/**")
    .excludePathPatterns("/user/login", "/user/register");
```

## 三、完成情况

| 模块 | 进度 | 备注 |
|------|------|------|
| 用户注册登录 | 100% | 接口开发完成 |
| JWT认证 | 100% | Token机制实现 |
| 拦截器 | 100% | 登录和管理员拦截 |

## 四、完成情况

### API接口清单
1. POST /user/register - 用户注册
2. POST /user/login - 用户登录
3. POST /user/reset-password - 重置密码
4. GET /user/info - 获取用户信息

## 五、遇到的问题与解决方案

**问题1**：JWT依赖在JDK 11+环境缺少JAXB支持  
**解决**：添加javax.xml.bind和jaxb-runtime依赖

**问题2**：拦截器中获取请求参数异常  
**解决**：使用RequestUtil工具类统一处理请求信息

## 六、明日计划

1. 开发分类管理模块（增删改查）
2. 开发电子书管理模块
3. 实现文档树形结构展示
