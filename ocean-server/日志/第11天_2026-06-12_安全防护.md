# 工作日志 - 第11天

**姓名**：宋明杰  
**组别**：第20组  
**日期**：2026年6月12日

## 一、今日工作内容

### 1. 安全防护开发
- ✅ 开发XssFilter XSS过滤器
- ✅ 开发XssFilterUtil工具类
- ✅ 实现请求参数XSS清理
- ✅ 使用Jsoup库过滤恶意脚本

### 2. 接口限流
- ✅ 引入Guava RateLimiter
- ✅ 开发RateLimit自定义注解
- ✅ 开发RateLimitAspect限流切面
- ✅ 实现基于用户的限流控制

### 3. 日志记录
- ✅ 开发LogInterceptor日志拦截器
- ✅ 开发LogAspect日志切面
- ✅ 记录请求方法、参数、响应时间
- ✅ 记录异常信息

### 4. 全局异常处理
- ✅ 完善GlobalExceptionHandler
- ✅ 统一异常响应格式
- ✅ 区分业务异常和系统异常
- ✅ 记录异常日志

## 二、技术实现要点

### XSS过滤
```java
@Component
public class XssFilter implements Filter {
    
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) {
        HttpServletRequest httpRequest = (HttpServletRequest) request;
        XssHttpServletRequestWrapper xssRequest = new XssHttpServletRequestWrapper(httpRequest);
        chain.doFilter(xssRequest, response);
    }
}

public class XssHttpServletRequestWrapper extends HttpServletRequestWrapper {
    
    @Override
    public String getParameter(String name) {
        String value = super.getParameter(name);
        return XssFilterUtil.clean(value);
    }
    
    @Override
    public String[] getParameterValues(String name) {
        String[] values = super.getParameterValues(name);
        if (values == null) return null;
        
        String[] cleanValues = new String[values.length];
        for (int i = 0; i < values.length; i++) {
            cleanValues[i] = XssFilterUtil.clean(values[i]);
        }
        return cleanValues;
    }
}

public class XssFilterUtil {
    public static String clean(String input) {
        if (input == null) return null;
        // 使用Jsoup清理HTML标签
        return Jsoup.clean(input, Safelist.none());
    }
}
```

### 接口限流
```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    int permitsPerSecond() default 10;
}

@Aspect
@Component
public class RateLimitAspect {
    
    private ConcurrentHashMap<String, RateLimiter> rateLimiterMap = new ConcurrentHashMap<>();
    
    @Around("@annotation(rateLimit)")
    public Object around(ProceedingJoinPoint point, RateLimit rateLimit) throws Throwable {
        String key = RequestUtil.getIp();
        RateLimiter limiter = rateLimiterMap.computeIfAbsent(key, 
            k -> RateLimiter.create(rateLimit.permitsPerSecond()));
        
        if (!limiter.tryAcquire()) {
            throw new BusinessException("请求过于频繁，请稍后再试");
        }
        
        return point.proceed();
    }
}
```

### 日志记录
```java
@Aspect
@Component
public class LogAspect {
    
    @Around("@annotation(org.springframework.web.bind.annotation.RequestMapping) || " +
            "@annotation(org.springframework.web.bind.annotation.GetMapping) || " +
            "@annotation(org.springframework.web.bind.annotation.PostMapping)")
    public Object around(ProceedingJoinPoint point) throws Throwable {
        long startTime = System.currentTimeMillis();
        
        String methodName = point.getSignature().getName();
        Object[] args = point.getArgs();
        
        log.info("开始调用方法: {}, 参数: {}", methodName, JSON.toJSONString(args));
        
        Object result = point.proceed();
        
        long endTime = System.currentTimeMillis();
        log.info("方法调用完成: {}, 耗时: {}ms", methodName, endTime - startTime);
        
        return result;
    }
}
```

## 三、完成情况

| 模块 | 进度 | 备注 |
|------|------|------|
| XSS防护 | 100% | 参数过滤完成 |
| 接口限流 | 100% | 基于IP限流 |
| 日志记录 | 100% | AOP切面日志 |
| 异常处理 | 100% | 统一响应格式 |

## 四、遇到的问题与解决方案

**问题1**：XSS过滤影响富文本内容  
**解决**：对笔记、文档等内容字段不进行XSS过滤，仅在用户昵称、评论等字段过滤

**问题2**：限流粒度太粗（基于IP）  
**解决**：后续可优化为基于用户ID限流，登录用户使用用户ID，未登录使用IP

**问题3**：日志记录影响性能  
**解决**：使用异步日志记录，避免阻塞主业务流程

## 五、明日计划

1. 开发竞赛答题模块数据库设计
2. 设计题库、答题记录、成绩表
3. 录入海洋知识竞赛题目
